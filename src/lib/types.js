// TODO: Can we have types for the model (like an enum to ensure types are correct)
// Should this also include the top-level sidebar classes?
export const ModalTypes = [
  'NONE',
  'CHATS',
  'CONTACTS'
]